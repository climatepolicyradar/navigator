name: main push checks

on:
  push:
    branches:
      - main


jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Check contains no FIXME comments
        run: grep -r FIXME . && exit 1
